spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tsb
    username: postgres
    password: mysecretpassword
  jpa:
    generate-ddl: true
    show-sql: true
  redis:
    host: localhost
    port: 6379
    pool:
      max-idle: 8
      min-idle: 0
      max-active: 8
      max-wait: 1
